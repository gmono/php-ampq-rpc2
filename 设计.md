# 队列
全部使用direct交换机

1. 泛调用队列  使用发布订阅模式，对应multiCall函数，支持连续返回模式 返回observable  ，一次返回模式，自动搜集observable在限定时间内，返回数组
2. 随机调用队列() 使用生产消费模式 ，对应call函数，返回单一对象
3. 通用返回队列，所有返回消息只通过一个result队列返回，所有返回消息只有一个callid和一个serviceid，泛调用中的所有返回消息的callid相同，serviceid不同。
4. 全局事件总线，用于事件通知，独立，由RemoteBusClient类来对接，使用received 函数来监听事件返回observable
   1. 对象:RemoteEventMessage(eventid,senderid,data)


## 全局队列
1. 全局通道prefix(_generic)
   1. 全局泛调用队列(call)
   2. 全局随机调用队列(random_call)
2. 特定调用的返回队列 (xxx_result)
# 服务注册
1. 每个队列可保存
  1. 一个服务注册表 ，dict格式 ，serviceid->ServiceInfo