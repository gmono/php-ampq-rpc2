#说明
基于简单通道对的纯MQ 适合CS架构

# 基本架构
1. 经典调用返回结构
2. 单队列调用单队列返回
3. 当使用全节点调用时 ，所有节点都连接到fanout交换机，并同时接收命令 同时返回结果
4. 支持超时时间，特别是全节点调用时
5. 支持search，可以在N秒内把所有得到响应的节点都收录，此后可以单独观察某个节点的状态


# 无协程架构
1. 使用命令发送+http返回
2. http提供一个putResult接口，参数为一个json格式，Post方式，使用body传递
3. 库函数使用无promise无返回值的模式，直接调用并等待
4. 超时使用主动超时即
   1. 当putResult的时候会检查对应callid对应的发起时间，如果超过时间则不执行回调。
   2. 使用一个callid回调表，putResult会执行调用并